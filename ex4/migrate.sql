-- Script to fix simpson.db
-- either do step by step, or design the new tables first, then move data into them, then remove old

-- Step 1. Rename tables, remove simpsons_ prefix

-- Fix datatypes

-- Fix naming, e.g. id to customer_id

-- Fix primary keys

-- Fix foreign keys and their datatypes